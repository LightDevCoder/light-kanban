# 2026-08-16

## Light-Kanban v1.0.2 UI/UX 重构（按用户 SPEC 执行）

- 环境：git 已恢复可用（2.50.1）；`brew install go` 装了 Go 1.26.6；前端用 WorkBuddy 托管 node 22（`~/.workbuddy/binaries/node/versions/22.22.2/bin`）。
- Clone 仓库到工作区（目录非空：clone 到 /tmp 再 rsync 回来），baseline 全绿（go test/vet + curl 全生命周期冒烟）。
- 后端（向后兼容附加）：`POST /block` 可选 `{"reason"}` → `block_reason`（unblock 清除）；`POST /reject` 可选 `{"feedback"}` → `review_feedback`（complete 清除）；SetStatus 人工纠正清空两者。红绿纪律：先加测试再实现。
- 前端：vanilla JS → `frontend/`（React 18 + TS + Vite 5 + TanStack Query v5，无组件库，手写 CSS 对齐参考图）。产物拷入 `internal/webui/dist/` 并**提交进 git**（fresh clone 无需 npm 即可 go build/test）；webui.go 改 `embed all:dist` + fs.Sub。旧 index.html/app.js/style.css 已删。
- 新 UI：50px 浅色顶栏（搜索/筛选/设置/+）、四列 pinned 列头 + 独立滚动 + 窄屏横滚、紧凑卡（LK-XXXX 短号=id 前4位hex大写、右上头像、workspace basename+哈希色点、≤2标签+N、due/overdue/stuck chips、blocked reason 行）、右侧 420px 抽屉（view/edit 分离；验收通过/退回修改带反馈/回收/删除/打开文件夹）、Archive/Guide/语言收进设置菜单、断连才显示提示。双语保留，5s 轮询保留。
- 验证：go test/vet ✅；npm build ✅；embed 二进制 ✅；playwright-core（channel:'chrome'，驱动本机 Chrome 免下载）交互 QA 12 项全过（搜索/退回反馈/验收/回收/创建/删除/语言切换/编辑+状态纠正/文件夹浏览/workspace 筛选）。
- 演示/QA 环境：`/tmp/lk-demo`（运行目录，数据不入库）+ `scripts/seed-demo.cjs`（35 任务/3 Agent/4 workspace）；stuck 演示用 `sqlite3 kanban.db` 把某 in_progress 的 updated_at 改老（RFC3339 格式）。
- 文档全量同步：README×2、AGENTS、CONTEXT、PROGRESS、spec.md、验收清单（76 行，xlsx 已重新生成）、新增 ADR-0002、cross-build.ps1 加前端构建步骤、Makefile 加 frontend-* 目标。
- 本地 commits：882e959（API 字段）→ 6ba1496（前端）→ d6875bb（文档）。**未 push**。
- 状态：进入 LOCAL ACCEPTANCE。服务常驻 http://localhost:8080（/tmp/lk-demo，task Fhf0UR）。等用户验收 + 截中英文图（存 Assets/light-kanban-CN.png / -EN.png，替换旧 example.png）→ 用户明确批准后才 push/tag v1.0.2/发 Release。
- 注意：git commit 身份是本机自动值 "Light Cheng <light@LightBookPro.local>"；发布前可能要用 gh 账号（LightDevCoder）配置 user.name/email。
