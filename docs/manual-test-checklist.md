# Light-Kanban 手动验收清单

> 照着下表把功能都点一遍，在「结果」列打 ✅ / ❌；想给我留言就写在「评论（留给我）」列，然后告诉我一声，我会读这个文件逐条回复。
> 想用 Excel 测：打开 `docs/light-kanban-验收清单.xlsx`（同一份清单，「结果」列带 ✅ 通过 / ❌ 失败 / ⚠ 存疑 下拉选项）。改动清单后可用 `node scripts/make-checklist-xlsx.cjs` 重新生成。
> 前提：用最新二进制启动服务（`./dist/light-kanban -addr :8080`），浏览器打开 http://localhost:8080 并**硬刷新**（Ctrl+F5 / Cmd+Shift+R，避免旧页面缓存）。
> 想看高密度效果：服务运行中执行 `node scripts/seed-demo.cjs` 灌入 35 条演示任务。

## A. 启动与页面

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| A1 | 服务启动 | 运行 `./dist/light-kanban -addr :8080` | 命令行出现 `listening on :8080`，不报错 | |
| A1b | 双击启动（Windows） | 直接双击 `dist\light-kanban.exe` | 弹出控制台窗口并**自动打开浏览器** http://localhost:8080 | |
| A2 | 健康检查 | 浏览器开 http://localhost:8080/api/health | 显示 `{"db":"ok","status":"ok"}` | |
| A3 | 页面加载 | 打开 http://localhost:8080 | 浅色顶栏（Light Kanban / 任务看板）+ 四列看板，无弹窗遮盖，无深色大 Header | |
| A4 | 连接指示 | 正常时看顶栏右侧 | **没有**常亮绿色状态点；停掉服务后出现红色「连接失败」 | |
| A5 | 首次引导 | 新开无痕窗口（或清掉 localStorage `lk-wizard-seen`）打开页面 | 自动弹出 4 步引导向导，可上一步 / 下一步 / 跳过 | |
| A6 | 重开向导 | 跳过向导 → 顶栏右侧设置（齿轮）→「使用向导」 | 向导随时能重新打开 | |
| A6b | 语言切换 | 设置菜单 →「界面语言」 | 整个界面（列名、按钮、弹窗、抽屉、向导）在中英文间即时切换；刷新后保持 | |
| A7 | 不再自动弹出 | 点「跳过」或「开始使用」后刷新页面 | 刷新后不再自动弹出 | |

## B. 新建任务

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| B1 | 顶栏新建 | 点顶栏右侧「+」→ 填标题和 workspace 路径 → 创建 | 弹窗关闭，任务出现在**待处理**列 | |
| B1b | 列头快速新增 | 点待处理列标题右侧「+」 | 同样打开新建弹窗，创建后进入待处理列 | |
| B2 | 缺标题 | 弹窗只填路径，点创建 | 弹「创建任务失败：title is required」 | |
| B3 | 缺路径 | 弹窗只填标题，点创建 | 弹「创建任务失败：workspacePath is required」 | |
| B4 | 可选字段 | 填描述、标签、截止时间 | 卡片显示标签、截止提示；描述在抽屉里完整可见 | |
| B5 | 多个标签 | 标签填 `go, ui, test` | 卡片最多显示 2 个标签 + 「+1」 | |
| B6 | 截止时间留空 | 不填截止时间 | 能正常创建，卡片无截止提示 | |
| B7 | 页内浏览选目录 | 点路径框旁「浏览…」→ 点进文件夹 →「选择此文件夹」 | 路径框自动填入所选文件夹完整路径 | |
| B8 | 浏览上级 | 浏览弹窗里点「上级」 | 回到上一级目录；到根目录后 Mac 显示 `/` | |
| B12 | 取消新建 | 打开弹窗后点「取消」/ 点弹窗外 / 按 Esc | 弹窗关闭，没有任务被创建 | |

## C. 看板布局

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| C1 | 四列结构 | 看主页面 | 待处理(灰○) / 处理中(黄◐) / 遇到阻碍(红!) / 等你确认(绿✓)，列头右侧有任务计数 | |
| C2 | 列独立滚动 | 灌入演示数据后滚动某一列 | 每列独立上下滚动，页面整体不随任务增多向下增长 | |
| C3 | 列头固定 | 列内滚动时看列头 | 列标题、计数、快速新增按钮始终停在列顶 | |
| C4 | 窄屏横滚 | 把窗口拖窄（< ~1300px） | 看板出现横向滚动，四列保持原宽度不重排 | |
| C5 | 高密度 | 一屏内数卡片 | 同样屏幕能放下明显比旧版更多的卡片，且不拥挤 | |

## D. 卡片信息

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| D1 | 短号与头像 | 看任意卡片第一行 | 左侧淡灰 `LK-XXXX` 短号，右侧 18px agent 头像（未接取的卡片没有头像） | |
| D2 | workspace 显示 | 看卡片底部 | 只显示文件夹名（如 light-kanban）+ 颜色点，**不显示**完整路径；悬停可见全路径 | |
| D3 | 标签折叠 | 找一张 3+ 标签的卡 | 最多 2 个标签 + 「+N」，不撑高卡片 | |
| D4 | 截止提示 | 演示数据里找今天截止 / 已逾期卡片 | 分别显示黄色「今天截止」/ 红色「已逾期」；未来截止显示灰色短日期 | |
| D5 | 卡住标记 | 处理中超过 24h 未更新的任务 | 卡片底部出现「⚠ 疑似卡住」，无红框无闪烁 | |
| D6 | 阻碍原因 | 看遇到阻碍列卡片 | 卡片上直接以红色「! 原因」显示 agent 填的 blockReason | |
| D7 | 长标题 | 找一张长标题卡 | 标题最多两行截断，卡片不被撑爆 | |

## E. 任务抽屉

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| E1 | 打开抽屉 | 点任意卡片 | 右侧滑出抽屉，看板仍部分可见；含短号、标题、状态、Agent、workspace、描述、标签、截止、创建/更新时间 | |
| E2 | 关闭抽屉 | 点 × / 点抽屉外 / 按 Esc | 抽屉关闭，回到原看板位置 | |
| E3 | 查看/编辑分离 | 打开抽屉 | 默认是查看模式，没有输入框；点「编辑」才进入编辑 | |
| E4 | 编辑保存 | 编辑标题/描述/标签 → 保存 | 回到查看模式，卡片内容更新 | |
| E5 | 人工纠正状态 | 编辑里把状态从待处理改成等你确认 → 保存 | 卡片直接移到等你确认列 | |
| E6 | 清空可选字段 | 编辑里把标签、截止时间清空 → 保存 | 卡片上的标签、截止提示消失 | |
| E7 | 打开项目文件夹 | 抽屉里 workspace 行点文件夹图标 | 服务器那台电脑弹出访达/资源管理器定位到该文件夹 | |
| E8 | 删除任务 | 抽屉里点「删除」→ 确认 | 卡片从看板消失，归档历史里也没有 | |

## F. 验收（等你确认）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| F1 | 悬停验收 | 悬停等你确认列的卡片 | 卡片底部浮现「验收通过」「退回修改」两个按钮 | |
| F2 | 验收通过 | 点「验收通过」（悬停按钮或抽屉内） | 卡片从四列消失；设置 → 归档历史里能看到，带「完成于 …」时间 | |
| F3 | 退回修改带反馈 | 抽屉里点「退回修改」→ 填反馈 → 确认退回 | 卡片回到处理中列；抽屉里可见「退回反馈」；`curl http://localhost:8080/api/tasks` 能读到 `reviewFeedback` 字段 | |
| F4 | 反馈流转 | 退回后让 agent 重新 complete（或编辑改状态到等你确认） | 退回反馈被清除，新一轮验收重新开始 | |

## G. 接取任务 & agent 身份（API）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| G1 | 主界面无注册入口 | 看主页面 | 页面上**没有** Agent 注册表单；agent 信息只出现在卡片/抽屉里 | |
| G2 | API 注册并接取 | 先 `curl -F "file=@头像.png" .../api/avatars` 记 path，再 `curl -X POST -d '{"agentId":"a1","name":"Alpha","avatar":"<path>"}' .../api/tasks/<id>/claim` | 返回 200，卡片移到处理中列，右上角显示头像 | |
| G3 | 界面不显示 agentId | 看卡片与抽屉 | 只显示头像 + 名称，**不显示** agentId（仅存在于 API 层） | |
| G4 | 缺名称/头像被拒 | claim 缺 `name`、头像用字母/emoji、或伪造 `/api/avatars/xxx.png` | 返回 422，任务不被接取 | |
| G5 | 抢同一任务 | 两个不同 agentId 先后 claim 同一任务 | **只有一个成功**，另一个 409 | |

## H. 状态流转（agent 通过 API，界面无按钮）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| H1 | 无 agent 操作按钮 | 看处理中/遇到阻碍列卡片与抽屉 | 没有「阻碍/完成/解除阻碍」按钮（这些是 agent 的 API 动作） | |
| H2 | API 状态流转 | curl 调 `POST /api/tasks/<id>/block`、`unblock`、`complete` | 卡片在列间正确移动 | |
| H3 | block 带原因 | `curl -X POST -d '{"reason":"等 token"}' .../block` | 卡片与抽屉显示该原因；unblock 后消失 | |

## I. 回收孤儿任务

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| I1 | 抽屉回收 | 处理中卡片打开抽屉 →「回收到待处理」 | 卡片回到待处理列，agent 头像消失 | |
| I2 | 回收后重新接取 | 回收后由 agent 重新 claim | 能成功接取 | |
| I3 | API 回收 | `curl -X POST .../api/tasks/<id>/recycle` | 同样回到待处理 | |

## J. 归档历史

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| J1 | 入口位置 | 看顶栏 | 顶栏没有「归档历史」主按钮；在设置（齿轮）菜单里打开 | |
| J2 | 历史详情 | 打开归档历史 | 显示标题、workspace、标签、完成时间 | |
| J3 | 单条删除 | 点某条「删除」→ 确认 | 该条消失 | |
| J4 | 全选删除 | 勾选若干条（或全选）→「删除选中」→ 确认 | 选中条目全部消失 | |

## K. 搜索与筛选

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| K1 | 搜索标题 | 顶栏搜索框输入标题关键词 | 看板就地过滤，四列结构保持 | |
| K2 | 搜索 workspace | 输入文件夹名（如 light-kanban） | 该 workspace 的任务被筛出（全路径与简称都能命中） | |
| K3 | 搜索 Agent | 输入 agent 名称 | 该 agent 持有的任务被筛出 | |
| K4 | 组合筛选 | 筛选里勾 Workspace=light-kanban + 标签=ui | 看板只显示同时匹配的任务，筛选按钮显示激活数量 | |
| K5 | 清除筛选 | 点「清除全部」 | 看板恢复完整 | |

## L. Agent 预配置（API）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| L1 | 预配置 agent | `curl -X POST -d '{"id":"a1","name":"Alpha"}' .../api/agents` | 成功；`curl .../api/agents` 能看到 | |
| L2 | 更新头像 | 上传头像后 POST /api/agents 带 `avatar` | 之后该 agent 接取的卡片显示新头像 | |
| L3 | 身份钉死 | 预配置后该 agent claim 时带别的名称/头像 | 卡片仍显示预配置的名称和头像 | |

## M. API 抽查（可选，用命令行）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| M1 | 健康检查 | `curl http://localhost:8080/api/health` | `{"db":"ok","status":"ok"}` | |
| M2 | 创建任务 | `curl -X POST -d '{"title":"API 测试","workspacePath":"/tmp/x"}' .../api/tasks` | 返回任务 JSON，`"status":"todo"` | |
| M3 | 接取冲突 | 对同一任务连发两次 claim | 第一次 200，第二次 409 | |
| M4 | 归档历史查询 | `curl ".../api/tasks?status=archived"` | 返回已归档数组，含 `completedAt` | |
| M5 | 目录浏览 | `curl ".../api/fs/dirs?path=%2F"`（Mac） | 返回该目录下的子文件夹列表 | |

## N. 跨平台（Mac）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| N1 | Apple Silicon | `chmod +x` 后运行 `./light-kanban-darwin-arm64 -addr :8080` | 正常启动，页面功能同 Windows | |
| N2 | Intel Mac | 用 `dist/light-kanban-darwin-amd64` | 同上 | |
| N3 | Gatekeeper | 首次运行若提示"无法验证开发者" | 右键 → 打开，或 `xattr -dr com.apple.quarantine ./light-kanban-darwin-arm64` 后重试 | |

---

**判读**：A~L 全部 ✅ 即核心功能验收通过；M、N 为抽查项。任一 ❌ 请把编号和现象发回来。
