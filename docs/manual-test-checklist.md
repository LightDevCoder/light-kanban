# Light-Kanban 手动验收清单

> 照着下表把功能都点一遍，在「结果」列打 ✅ / ❌；想给我留言就写在「评论（留给我）」列，然后告诉我一声，我会读这个文件逐条回复。
> 想用 Excel 测：打开 `docs/light-kanban-验收清单.xlsx`（同一份清单，「结果」列带 ✅ 通过 / ❌ 失败 / ⚠ 存疑 下拉选项）。改动清单后可用 `node scripts/make-checklist-xlsx.cjs` 重新生成。
> 前提：用最新二进制启动服务（`dist\light-kanban.exe -addr :8080`），浏览器打开 http://localhost:8080 并**硬刷新**（Ctrl+F5 / Cmd+Shift+R，避免旧页面缓存）。

## A. 启动与页面

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| A1 | 服务启动 | 运行 `dist\light-kanban.exe -addr :8080` | 命令行出现 `listening on :8080`，不报错 | |
| A1b | 双击启动 | 直接双击 `dist\light-kanban.exe` | 弹出控制台窗口并**自动打开浏览器** http://localhost:8080，控制台打印访问地址 | |
| A2 | 健康检查 | 浏览器开 http://localhost:8080/api/health | 显示 `{"db":"ok","status":"ok"}` | |
| A3 | 页面加载 | 打开 http://localhost:8080 | 出现「Light-Kanban 任务看板」标题，**没有**弹窗盖住页面 | |
| A4 | 连接指示 | 看右上角圆点 | 绿色 ● | |
| A5 | 首次引导 | 新开无痕窗口（或清掉 localStorage 里的 `lk-wizard-seen`）后打开页面 | 自动弹出「欢迎使用」引导向导，共 4 步，可「上一步 / 下一步 / 跳过」 | |
| A6 | 重开向导 | 向导里点「跳过」→ 点右上「使用向导」 | 向导关闭后，「使用向导」随时能重新打开 | |
| A6b | 语言切换 | 点顶栏「EN / 中文」按钮 | 整个界面（列名、按钮、弹窗、向导）在中英文间即时切换；刷新后保持所选语言 | |
| A7 | 不再自动弹出 | 点「跳过」或「开始使用」后刷新页面 | 刷新后不再自动弹出（本浏览器记住；想要再看点「使用向导」） | |

## B. 添加任务

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| B1 | 正常添加 | 点右上「＋ 添加任务」→ 弹窗填「任务标题」和「workspace 文件夹路径」→ 点「添加」 | 弹窗关闭，任务出现在**待处理**列（灰圈图标） | |
| B2 | 缺标题 | 弹窗只填路径，点添加 | 弹「添加任务失败：title is required」 | |
| B3 | 缺路径 | 弹窗只填标题，点添加 | 弹「添加任务失败：workspacePath is required」 | |
| B4 | 可选字段 | 添加时填描述、标签、截止时间 | 卡片上显示紫色标签、黄色「截止…」提示、描述文本 | |
| B5 | 多个标签 | 标签填 `go, ui` | 显示两个紫色标签 | |
| B6 | 截止时间留空 | 添加时不填截止时间 | 能正常添加，卡片无截止提示 | |
| B7 | 页内浏览选目录 | 点路径框旁「浏览…」→ 点进文件夹 → 点「选择此文件夹」 | 路径框自动填入所选文件夹的完整路径 | |
| B8 | 浏览上级 | 浏览弹窗里点「上级」 | 回到上一级目录；到根目录后显示 `C:\`、`D:\`（Mac 显示 `/`） | |
| B9 | 系统选择 | 点「系统选择…」 | 服务器所在电脑弹出系统文件夹对话框，选完路径自动填入 | |
| B10 | 添加多个任务 | 连续添加 2~3 个 | 都出现在待处理列，列标题右侧计数正确 | |
| B11 | 直达项目目录 | 点任意卡片标题旁的 📁 按钮 | 服务器那台电脑弹出资源管理器/访达，定位到该任务的 workspace 文件夹；路径不存在时提示错误 | |
| B12 | 取消添加 | 点「＋ 添加任务」→ 点「取消」或点弹窗外区域 | 弹窗关闭，没有任务被创建 | |

## C. 四列看板

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| C1 | 列标题图标 | 看四个列标题前的圆圈 | 待处理=灰色空心圆，处理中=黄色圆(…)，遇到阻碍=红色圆(!)，等你确认=绿色圆(✓) | |
| C2 | 无整条色带 | 看列头背景 | 列头是白底细边，不是一整条颜色 | |

## D. 接取任务 & agent 身份

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| D1 | 主界面无注册入口 | 看主页面 | 页面上**没有**「Agent 管理 / 注册」表单；agent 信息只出现在被它接取的卡片上 | |
| D2 | API 注册并接取 | 先上传头像：`curl -F "file=@头像.png" http://localhost:8080/api/avatars`，记下返回的 `path`；再 claim：`curl -X POST -H "Content-Type: application/json" -d '{"agentId":"a1","name":"Alpha","avatar":"<上一步的path>"}' http://localhost:8080/api/tasks/<id>/claim` | 返回 200，卡片移到**处理中**列，标题旁显示图片头像 + 名称 | |
| D3 | 卡片只显示名称 | 用名称 `Grok Build`、id `grok build` 接取后看卡片 | 卡片只显示图片头像 + 加粗名称「Grok Build」，**不显示** id（id 仅存在于 API 层） | |
| D4 | 图片头像 | 接取后看卡片 | 显示上传的图片（圆形裁剪），不是字母/表情符号 | |
| D5 | 缺名称/头像被拒 | API 接取时缺 `name`、头像不是图片（如 `G`、emoji）、或 `avatar` 指向不存在的文件（伪造 `/api/avatars/xxx.png`） | 返回 422 并提示，任务不被接取，不会出现坏图 | |
| D6 | 多个 agent | 换一个 agentId 接取另一个任务 | 每个卡片显示各自的 agent 头像 | |
| D7 | 抢同一任务 | 两个不同 agentId 先后 claim 同一任务（或开两个浏览器窗口同时点） | **只有一个人成功**，另一个 409 + 提示 | |

## E. 状态流转（agent 通过 API 做，界面无按钮）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| E1 | 处理中/阻碍卡片按钮 | 看「处理中」「遇到阻碍」列的卡片 | 只显示「编辑」「删除」，没有「阻碍/完成/回收/解除阻碍」按钮 | |
| E2 | API 状态流转 | 用 curl 调 `POST /api/tasks/<id>/block`、`unblock`、`complete` | 卡片在列间正确移动（这是 agent 的动作，界面不提供按钮） | |

## F. 验收（等你确认）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| F1 | 验收通过 | 等你确认的卡片点「验收通过（归档）」 | 卡片从四列消失；点右上「归档历史」→ 弹窗里能看到它，带「完成于 …」时间 | |
| F2 | 无退回修改按钮 | 看「等你确认」卡片 | 只有「验收通过（归档）」「编辑」「删除」，没有「退回修改」——要退回时直接命令 agent，agent 自己调 API 把任务退回「处理中」 | |
| F3 | 历史详情 | 归档历史弹窗里看条目 | 显示标题、标签、完成时间 | |
| F4 | 历史关闭 | 点「关闭」或点击弹窗外区域 | 弹窗关闭，看板恢复正常 | |
| F5 | 单条删除 | 历史弹窗里点某条「删除」→ 确认 | 该条消失，看板和历史里都不再有 | |
| F6 | 全选删除 | 历史弹窗勾选若干条（或勾「全选」）→ 点「删除选中」→ 确认 | 选中的条目全部消失 | |

## G. 编辑任务

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| G1 | 编辑字段 | 任意卡片点「编辑」→ 改标题/描述/标签 → 保存 | 卡片内容更新 | |
| G2 | 人工纠正状态 | 编辑弹窗把「状态」从待处理改成等你确认 → 保存 | 卡片直接移到等你确认列 | |
| G3 | 清空可选字段 | 编辑里把标签清空 → 保存 | 卡片上的标签消失 | |
| G4 | 删除任务 | 任意卡片点「删除」→ 确认弹窗 | 卡片从看板消失，归档历史里也没有；同一任务再删提示不存在 | |
| G5 | 编辑已归档任务 | 历史里没有编辑入口 | 归档历史只读（符合预期） | |

## H. Agent 身份（API 预配置）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| H1 | 预配置 agent | `curl -X POST -H "Content-Type: application/json" -d '{"id":"a1","name":"Alpha"}' http://localhost:8080/api/agents` | 成功；`curl http://localhost:8080/api/agents` 能看到 a1/Alpha | |
| H2 | 更新头像 | 先上传头像（`curl -F "file=@x.png" http://localhost:8080/api/avatars`），再 POST /api/agents 带 `avatar` | 之后该 agent 接取的卡片显示新头像 | |
| H3 | 空名称预配置 | `curl -X POST -H "Content-Type: application/json" -d '{"id":"a1"}' http://localhost:8080/api/agents` | 名称自动等于 id | |
| H4 | 身份钉死 | 预配置后，该 agent claim 时带别的名称/头像 | 卡片仍显示预配置的名称和头像（不被 agent 覆盖） | |

## I. 回收孤儿任务

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| I1 | 回收孤儿任务 | 处理中卡片点「编辑」→ 状态改为「待处理」→ 保存（或 `curl -X POST http://localhost:8080/api/tasks/<id>/recycle`） | 卡片回到**待处理**列，agent 头像消失 | |
| I2 | 回收后重新接取 | 回收后再点「接取」 | 能成功接取 | |
| I3 | 疑似卡住标记 | 处理中卡片超过 24 小时未更新（快速验证：把 `internal/webui/app.js` 里 `STUCK_THRESHOLD_MS` 临时改成 `60000` 重启，等 1 分钟） | 卡片红框 + 红色「疑似卡住」闪烁标记 | |

## J. API 抽查（可选，用命令行）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| J1 | 健康检查 | `curl http://localhost:8080/api/health` | `{"db":"ok","status":"ok"}` | |
| J2 | 创建任务 | `curl -X POST -H "Content-Type: application/json" -d '{"title":"API 测试","workspacePath":"C:/x"}' http://localhost:8080/api/tasks` | 返回任务 JSON，`"status":"todo"` | |
| J3 | 接取冲突 | 对同一任务连发两次 claim（`curl -X POST -d '{"agentId":"a1"}' http://localhost:8080/api/tasks/<id>/claim`） | 第一次 200，第二次 409 + error 信息 | |
| J4 | 归档历史查询 | `curl "http://localhost:8080/api/tasks?status=archived"` | 返回已归档任务数组，含 `completedAt` | |
| J5 | 目录浏览 | `curl "http://localhost:8080/api/fs/dirs?path=C%3A%5C"`（Mac 用 `?path=%2F`） | 返回该目录下的子文件夹列表 | |

## K. 跨平台（Mac）

| # | 测什么 | 怎么测 | 预期结果 | 结果 | 评论（留给我） |
|---|--------|--------|----------|------|--------------|
| K1 | Apple Silicon | 用 `dist/light-kanban-darwin-arm64`：`chmod +x` 后运行 `./light-kanban-darwin-arm64 -addr :8080` | 正常启动，页面功能同 Windows | |
| K2 | Intel Mac | 用 `dist/light-kanban-darwin-amd64` | 同上 | |
| K3 | Gatekeeper | 首次运行若提示"无法验证开发者" | 右键 → 打开，或 `xattr -dr com.apple.quarantine ./light-kanban-darwin-arm64` 后重试 | |
| K4 | 系统选择 | Mac 上点「系统选择…」 | 弹出 macOS 的 `choose folder` 对话框 | |

---

**判读**：A~I 全部 ✅ 即核心功能验收通过；J、K 为抽查项。任一 ❌ 请把编号和弹窗原文发回来。
